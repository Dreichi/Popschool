// Définissez une variable appelée `couleurs` et affectez-lui un tableau contenant les chaînes de caractères "rouge", "vert", "bleu", "jaune" et "orange".
// Utilisez une boucle for pour parcourir le tableau et afficher chaque élément dans la console en utilisant la fonction console.log().
let couleurs = ["rouge", "vert", "bleu", "jaune", "orange"];
for (let i = 0; i < couleurs.length; i++) {
  console.log(couleurs[i]);
}